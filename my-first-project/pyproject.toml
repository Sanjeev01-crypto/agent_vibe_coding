[project]
name = "expense-workflow"
version = "0.1.0"
description = "Ambient expense approval workflow built with Google ADK 2.0"
dependencies = [
    "google-adk>=2.0.0",
    "fastapi>=0.100.0",
    "uvicorn>=0.20.0",
    "pydantic>=2.0.0",
]
requires-python = ">=3.10"
